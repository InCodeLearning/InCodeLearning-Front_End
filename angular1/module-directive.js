/* a directive is a marker on HTML tag for angular to run or reference 
javascript code */

/* modules encapsulates pieces of angular application and definitions of 
dependencies for our app, which makes our code more maintainable, 
testable, and readable. */
var app = angular.module('store',[]);
/* expressions allow one to insert dynamic values into html 
e.g. {{4+6}} */
